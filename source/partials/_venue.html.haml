%section#venue
  %h3
    Venue and Program

  %figure
    %iframe(width="100%" height="250" frameborder="0" scrolling="no" marginheight="0" marginwidth="0" src="#{venue.google_maps_url}")

  .program
    %dl
      - program.items.each do |item|
        %dt
          = item.time
        %dd
          = item.description

  .venue
    %p.inline
      You can find us at
    %address
      :markdown
        #{venue.address}
    %p
      More information can be found online
      = succeed "." do
        %a(href="#{venue.url}" title="#{venue.url_title}")
          = venue.url_title
